<!-- <p id="notice"><%= notice %></p> -->

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>List Position:</strong>
  <%= @list.position %>
</p>

<% @list.cards.each do |card| %>
  <p>
    <strong>Card:</strong>
    <%= card.title %>
    <p> <strong>Card Position</strong>
      <%= card.position %>
    </p>
  </p>
<% end %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
